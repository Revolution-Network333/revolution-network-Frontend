{
  "name": "revolution-network",
  "version": "1.0.0",
  "description": "Root package for Revolution Network",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "build": "cd backend && npm install",
    "postinstall": "cd backend && npm install"
  },
  "dependencies": {
    "dotenv": "^16.3.1",
    "mysql2": "^3.15.2",
    "sharp": "^0.34.5"
  }
}
